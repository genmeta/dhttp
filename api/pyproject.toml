[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "dhttp"
dynamic = ["version"]
requires-python = ">=3.10"

[tool.maturin]
module-name = "dhttp"
features = ["pyo3", "pyo3/extension-module"]
